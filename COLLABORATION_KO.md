# GitHub + AI 협업 가이드

이 프로젝트의 기본 방식은 간단합니다.

> 사람은 목표를 정하고 리뷰합니다. AI는 코드를 찾고, 만들고, 테스트합니다.
> GitHub는 변경 기록과 대화를 보관합니다.

## 처음 한 번만 준비

프로젝트 소유자는 GitHub 저장소의 `Settings → Collaborators`에서 친구를
초대합니다. 친구는 초대 메일에서 참여를 수락합니다. 둘 다 Codex에서 이
프로젝트 폴더를 열고, 저장소의 `AGENTS.md`를 읽을 수 있어야 합니다.

가능하면 `main` 브랜치는 보호하고, 직접 push 대신 Pull Request(PR)만
병합하도록 설정합니다. 이 저장소는 PR마다 자동으로 테스트와 빌드를
실행하도록 설정되어 있습니다.

## 한 작업을 끝내는 순서

### 1. 할 일을 작게 정하기

GitHub `Issues`에 한 문장으로 적습니다.

예: “Archive 화면에서 날짜를 누르면 해당 리포트를 보여준다.”

큰 기능은 여러 Issue로 나눕니다. 한 PR에 여러 unrelated 기능을 넣지
않습니다.

### 2. 새 브랜치 만들기

`main`은 완성된 코드입니다. 각자 작업용 브랜치를 하나씩 만듭니다.

```bash
git switch main
git pull --ff-only
git switch -c feat/archive-report-link
```

이름은 `feat/기능`, `fix/버그`, `docs/문서`처럼 알아보기 쉽게 짓습니다.

### 3. Codex에게 작업시키기

Codex에 아래처럼 요청합니다.

```text
AGENTS.md를 먼저 읽어줘.
이번 작업은 “Archive 화면에서 날짜를 누르면 해당 리포트를 보여주기”야.
먼저 관련 파일과 현재 동작을 조사하고 짧은 계획을 말해줘.
범위를 넘는 리팩터링은 하지 말고 구현한 뒤 pnpm test && pnpm build를 실행해줘.
마지막에 변경 파일, 테스트 결과, 아직 확인하지 못한 위험을 알려줘.
```

AI가 만든 코드는 바로 믿지 말고 Codex에게 다음도 요청합니다.

```text
방금 변경한 git diff를 다시 리뷰해줘.
요구사항에서 빠진 것, 기존 동작을 깨는 부분, 보안 문제, 테스트가 부족한 부분을 찾아줘.
문제가 있으면 먼저 이유를 설명하고 수정해줘. 마지막에 검증 결과를 알려줘.
```

### 4. 커밋하고 GitHub에 올리기

작은 단위로 저장합니다.

```bash
git status
git add src/관련파일.tsx
git commit -m "feat: link archive reports"
git push -u origin feat/archive-report-link
```

Codex를 사용해도 커밋 전에는 변경 목록을 직접 확인합니다. `.env`, API
키, 쿠키, 개인 데이터가 보이면 올리지 않습니다.

### 5. Pull Request 만들기

GitHub에서 `Compare & pull request`를 누릅니다.

- 제목: 무엇을 바꿨는지 한 줄
- 설명: 왜 바꿨는지, 무엇이 달라졌는지
- 검증: `pnpm test`, `pnpm build` 결과
- 화면을 바꿨다면 전/후 스크린샷
- 관련 Issue가 있으면 `Closes #번호`

PR은 “완성품을 자랑하는 글”이 아니라 “이 변경을 같이 확인하는 공간”입니다.

### 6. 서로 리뷰하기

상대방은 다음을 봅니다.

- Issue의 요구사항을 정말 해결했는가?
- AI가 추측으로 만든 부분은 없는가?
- 기존 화면/API/테스트를 깨지 않는가?
- 비밀값이나 불필요한 파일이 들어가지 않았는가?
- CI가 통과했는가?

Codex로도 리뷰할 수 있습니다. 다만 AI 리뷰가 통과해도 사람이 최종
승인합니다.

### 7. 수정 요청 처리하기

리뷰에서 수정할 것이 있으면 **새 PR을 만들지 않습니다**. 같은 브랜치에서
Codex에게 리뷰 의견을 보여주고 수정한 뒤 다시 push합니다.

```bash
git add src/App.tsx
git diff --cached
git commit -m "fix: address review feedback"
git push
```

`src/App.tsx`는 예시입니다. 실제로 바꾼 파일만 넣고, `git diff --cached`로
커밋 직전 내용을 확인합니다. 그러면 기존 PR에 새 커밋과 변경 내용이
자동으로 나타납니다.

### 8. 병합하기

CI가 초록색이고, 리뷰어가 승인하고, 작성자가 diff를 마지막으로 확인한
뒤 `Squash and merge`를 사용합니다. 병합 후 브랜치를 삭제합니다.

다음 작업 전에는 항상 최신 `main`에서 새 브랜치를 시작합니다.

```bash
git switch main
git pull --ff-only
```

## 충돌이 생겼을 때

충돌은 두 사람이 같은 줄을 바꿨다는 뜻이지, 프로젝트가 망가졌다는 뜻은
아닙니다. Codex에게 “양쪽 변경의 목적을 설명하고 안전한 합쳐진 결과를
제안해줘”라고 요청합니다. 사람이 선택한 뒤 `pnpm test && pnpm build`를
다시 실행하고 push합니다.

## AI를 쓸 때 꼭 지킬 것

- API 키, 비밀번호, 쿠키, 세션 파일을 Codex나 GitHub에 붙여넣지 않습니다.
- AI가 수정한 diff를 읽고 커밋합니다.
- 테스트 통과만으로 기능이 완벽하다고 생각하지 않습니다. UI는 실제로 눌러봅니다.
- 운영 배포, 데이터 삭제, 설정 변경은 PR 리뷰 후 프로젝트 담당자가 합니다.
- 한 PR이 너무 커지면 AI에게 작은 PR 여러 개로 나눌 계획을 다시 세우게 합니다.

## 자주 쓰는 Codex 요청문

```text
이 PR의 변경만 기준으로 코드 리뷰를 해줘. 심각도 순서로 문제를 적고,
문제가 없으면 그렇게 말해줘. AGENTS.md의 프로젝트 규칙과 테스트 기준도 적용해줘.
```

```text
이 Issue를 구현하기 전에 현재 코드 흐름을 설명해줘. 수정 범위를 최소화하고,
테스트를 추가한 뒤 pnpm test && pnpm build까지 실행해줘.
```

```text
PR 리뷰에서 받은 아래 의견을 반영해줘. 먼저 어떤 파일을 왜 바꿀지 설명하고,
기존 동작을 보존하면서 수정한 뒤 diff와 테스트 결과를 요약해줘.
```
